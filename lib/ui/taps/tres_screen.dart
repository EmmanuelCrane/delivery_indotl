import 'package:flutter/material.dart';

class TresScreen extends StatelessWidget {
  const TresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Screen numero Tres"),
    );
  }
}
